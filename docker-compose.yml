services:
  redis:
    image: redis:7
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    restart: unless-stopped
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5000
      - FLASK_DEBUG=False
      - DATABASE_URL=sqlite:///data/database.db
      - REDIS_URL=redis://redis:6379/0
    ports:
      - "6161:5000"
    volumes:
      - ./:/app
      - ./logs:/app/logs
      - ./data:/app/data
    depends_on:
      - redis
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000/api/jobs || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 12

  worker:
    build: .
    command: celery -A app.tasks.celery_app worker --loglevel=info -c 1 -n worker@%h
    volumes:
      - ./:/app
      - ./logs:/app/logs
      - ./data:/app/data
    depends_on:
      - redis
      - web
    environment:
      - REDIS_URL=redis://redis:6379/0
      - PYTHONPATH=/app

  tester:
    build: .
    command: bash -c "bash ./scripts/wait_for_service.sh http://web:5000/api/jobs 120 && python scripts/e2e_test.py"
    depends_on:
      - web
      - worker
    volumes:
      - ./:/app
      - ./logs:/app/logs
      - ./data:/app/data
    environment:
      - REDIS_URL=redis://redis:6379/0
